---
description: 注释规范与边界
alwaysApply: false
---

# 注释规范

## 强制行为

- 仅在“代码本身无法直接表达意图”的场景添加注释：复杂逻辑、边界条件、业务约束、兼容性处理、性能/安全权衡
- 注释优先解释 **why / constraint / trade-off**，不是复述 **what**
- 多行注释优先采用 **JSDoc** 块注释格式（`/** ... */`）；极短的局部提示可使用单行注释
- 页面入口或组件文件中的注释，优先说明模块职责、关键交互约束、状态边界或拆分原因，不注释显而易见的 JSX 结构
- 页面或组件内存在大块条件渲染、数据映射、状态分支时，仅在读者难以直接看出意图的节点补充简短注释
- 导出的 TypeScript 类型、公共函数或跨模块 API 在“职责、约束、错误语义不直观”时应补充 JSDoc
- 存在副作用、错误语义或前置条件，且代码命名无法充分表达时，应在注释中明确输入约束与行为边界
- 注释必须与代码同步修改；逻辑变更时同时更新相关注释，避免失真
- 注释语言清晰、简洁、可执行，优先使用短句与项目术语

## 禁止行为

- 禁止保留大段无效注释代码
- 禁止使用“翻译式”低价值注释
- 禁止逐段解释 JSX 结构、样式类名或简单 props 透传
- 禁止为页面区块标题、普通表单项、常规列表渲染补充没有额外信息量的注释
- 禁止在同一代码块附近混用风格冲突的注释格式
- 禁止注释与实现不一致（过期注释、错误承诺、误导性描述）
- 禁止在注释中记录会快速过期的信息（临时时间点、无上下文 TODO）；必须改为可追踪事项（任务链接/issue）
- 禁止将注释作为掩盖坏命名或坏设计的手段；应优先重构命名与结构

## 推荐模板

- 类型注释：
  `/** <该类型在业务中的职责> */`
- 字段注释：
  `/** <字段业务含义/单位/可空语义> */`
- 函数注释：
  `/**`
  ` * <一句话说明用途与约束>`
  ` * @param <name> <参数语义/约束>`
  ` * @returns <返回值语义>`
  ` * @throws <抛错条件，可选>`
  ` */`
- 复杂逻辑注释：
  `/** <为何这样做 + 不这样做的风险> */`
- 兼容/兜底注释：
  `/** <兼容来源 + 触发条件 + 回退策略> */`
- 页面/组件顶层注释：
  `/** <该页面或组件的职责 + 关键交互/边界，按需添加> */`

## 快速自检

- 这条注释是否真正解释了“为什么”，而不是“代码做了什么”？
- 删除这条注释后，读者是否会误解约束、边界或风险？
- 代码变更后，这条注释是否仍然准确？
