---
globs: '**/*.{tsx,ts,jsx,js}'
description: 样式编写规范 - Tailwind CSS 使用、shadcn/ui 组件、响应式设计
---

# 样式编写规范

## 🎨 Tailwind CSS 使用规范

### 1. 基础使用

项目使用 **Tailwind CSS** 作为样式框架，所有样式通过 Tailwind 工具类实现。

```tsx
// ✅ 正确：使用 Tailwind 工具类
<div className="container mx-auto p-4">
  <div className="flex items-center justify-between">
    <h1 className="text-lg font-semibold">标题</h1>
    <button className="px-4 py-2 bg-primary text-white rounded-md hover:bg-primary/90">
      按钮
    </button>
  </div>
</div>

// ❌ 错误：不使用内联样式或自定义 CSS
<div style={{ padding: '16px' }}>
  <h1 style={{ fontSize: '18px' }}>标题</h1>
</div>
```

### 2. 使用 shadcn/ui 组件

项目使用 **shadcn/ui** 作为 UI 组件库，所有组件位于 `src/components/ui/` 目录。

```tsx
// ✅ 正确：使用 shadcn/ui 组件
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'

const MyPage = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>标题</CardTitle>
      </CardHeader>
      <CardContent>
        <Input placeholder="请输入" />
        <Button>提交</Button>
      </CardContent>
    </Card>
  )
}
```

### 3. 组合 Tailwind 类名

使用 `cn()` 工具函数（来自 `@/lib/utils`）组合类名：

```tsx
import { cn } from '@/lib/utils'

// ✅ 正确：使用 cn() 组合类名
<div className={cn('base-class', isActive && 'active-class', className)}>
  内容
</div>

// ✅ 正确：条件类名
<button
  className={cn(
    'px-4 py-2 rounded-md',
    variant === 'primary' && 'bg-primary text-white',
    variant === 'secondary' && 'bg-secondary text-secondary-foreground',
    disabled && 'opacity-50 cursor-not-allowed'
  )}
>
  按钮
</button>
```

### 4. 响应式设计

使用 Tailwind 的响应式前缀：

```tsx
// ✅ 正确：响应式类名
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  <div className="p-4 md:p-6 lg:p-8">内容</div>
</div>

// ✅ 正确：移动端优先
<div className="text-sm md:text-base lg:text-lg">文字</div>
```

### 5. 状态变体

使用 Tailwind 的状态修饰符：

```tsx
// ✅ 正确：状态类名
<button className="bg-primary hover:bg-primary/90 active:bg-primary/80 focus:outline-none focus:ring-2 focus:ring-primary">
  按钮
</button>

<a className="text-blue-600 hover:text-blue-800 visited:text-purple-600">
  链接
</a>
```

## 📦 shadcn/ui 组件使用

### 1. 可用组件

项目已配置的 shadcn/ui 组件：

- `Button` - 按钮组件
- `Card` - 卡片组件（CardHeader, CardTitle, CardContent, CardFooter）
- `Input` - 输入框组件
- `Label` - 标签组件
- `Form` - 表单组件（配合 react-hook-form）
- `Dialog` - 对话框组件
- `Avatar` - 头像组件
- `Toast` - 通过 `sonner` 实现

### 2. 组件位置

所有 UI 组件位于 `src/components/ui/` 目录，可以直接导入使用：

```tsx
import { Button } from '@/components/ui/button'
import { Card, CardContent } from '@/components/ui/card'
```

### 3. 添加新组件

如需添加新的 shadcn/ui 组件，使用 shadcn CLI：

```bash
npx shadcn@latest add [component-name]
```

或手动从 [shadcn/ui 官网](https://ui.shadcn.com/) 复制组件代码到 `src/components/ui/`。

## 🎯 字体大小规范

### 基础字体设置

- `html` 字体大小：16px（Tailwind 基准）
- `body` 字体大小：14px（PC 端标准）

### 字体大小层级

```tsx
// ✅ 正确：使用 Tailwind 字体大小类
<h1 className="text-lg font-semibold">标题</h1>        // 18px
<p className="text-base">正文</p>                      // 14px
<span className="text-sm text-muted-foreground">辅助文字</span>  // 12px
```

### 常用字体大小

| 类名 | 大小 | 用途 |
|------|------|------|
| `text-lg` | 18px | 标题 |
| `text-base` | 14px | 正文 |
| `text-sm` | 12px | 辅助文字 |
| `text-xs` | 10px | 极小文字 |

## 🎨 颜色使用

### 1. 使用主题颜色变量

项目使用 CSS 变量定义颜色，通过 Tailwind 配置使用：

```tsx
// ✅ 正确：使用主题颜色
<div className="bg-background text-foreground">
  <button className="bg-primary text-primary-foreground">
    主要按钮
  </button>
  <button className="bg-secondary text-secondary-foreground">
    次要按钮
  </button>
  <span className="text-muted-foreground">辅助文字</span>
</div>
```

### 2. 颜色变量

在 `src/styles/index.css` 中定义的颜色变量：

- `--background` / `--foreground` - 背景和前景色
- `--primary` / `--primary-foreground` - 主色
- `--secondary` / `--secondary-foreground` - 次色
- `--muted` / `--muted-foreground` - 静音色
- `--destructive` / `--destructive-foreground` - 危险色
- `--border` / `--input` / `--ring` - 边框和输入框颜色

## 📏 间距规范

### 使用 Tailwind 间距系统

```tsx
// ✅ 正确：使用 Tailwind 间距类
<div className="p-4">          // padding: 16px
<div className="m-2">          // margin: 8px
<div className="space-y-4">   // 子元素垂直间距 16px
<div className="gap-2">       // grid/flex 间距 8px
```

### 常用间距

| 类名 | 大小 | 用途 |
|------|------|------|
| `p-2` / `m-2` | 8px | 小间距 |
| `p-4` / `m-4` | 16px | 标准间距 |
| `p-6` / `m-6` | 24px | 大间距 |
| `p-8` / `m-8` | 32px | 超大间距 |

## 🎭 布局规范

### 1. 容器布局

```tsx
// ✅ 正确：使用容器类
<div className="container mx-auto p-4">
  {/* 内容 */}
</div>

// ✅ 正确：全屏布局
<div className="min-h-screen flex items-center justify-center">
  {/* 内容 */}
</div>
```

### 2. Flexbox 布局

```tsx
// ✅ 正确：Flex 布局
<div className="flex items-center justify-between">
  <div>左侧</div>
  <div>右侧</div>
</div>

<div className="flex flex-wrap gap-2">
  {/* 自动换行的 flex 布局 */}
</div>
```

### 3. Grid 布局

```tsx
// ✅ 正确：Grid 布局
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  {/* 响应式网格 */}
</div>
```

## 🚫 禁止事项

### ❌ 不要使用内联样式

```tsx
// ❌ 错误
<div style={{ padding: '16px', color: '#333' }}>内容</div>

// ✅ 正确
<div className="p-4 text-foreground">内容</div>
```

### ❌ 不要创建自定义 CSS 文件

```tsx
// ❌ 错误：不要创建 .module.css 或 .module.less
import styles from './index.module.css'

// ✅ 正确：使用 Tailwind 类名
<div className="p-4 bg-background">内容</div>
```

### ❌ 不要使用 !important

```tsx
// ❌ 错误：不要使用 !important
<div className="!p-8">内容</div>

// ✅ 正确：使用 Tailwind 的优先级或调整类名顺序
<div className="p-8">内容</div>
```

## 📚 最佳实践

### 1. 保持类名简洁

```tsx
// ✅ 推荐：简洁的类名
<button className="px-4 py-2 bg-primary text-white rounded-md">
  按钮
</button>

// ❌ 不推荐：过长的类名
<button className="px-4 py-2 bg-primary text-white rounded-md hover:bg-primary/90 active:bg-primary/80 focus:outline-none focus:ring-2 focus:ring-primary disabled:opacity-50 disabled:cursor-not-allowed">
  按钮
</button>
```

### 2. 使用组件封装重复样式

```tsx
// ✅ 推荐：封装为组件
const PrimaryButton = ({ children, ...props }) => {
  return (
    <Button
      className="px-4 py-2 bg-primary text-white rounded-md hover:bg-primary/90"
      {...props}
    >
      {children}
    </Button>
  )
}
```

### 3. 使用语义化的类名组合

```tsx
// ✅ 推荐：语义化的组合
<div className="flex items-center gap-4 p-6 bg-card rounded-lg border">
  {/* 卡片容器 */}
</div>
```

## 🔧 自定义样式

### 1. 扩展 Tailwind 配置

如需添加自定义颜色、间距等，在 `tailwind.config.js` 中扩展：

```js
export default {
  theme: {
    extend: {
      colors: {
        // 自定义颜色
      },
      spacing: {
        // 自定义间距
      },
    },
  },
}
```

### 2. 使用 CSS 变量

在 `src/styles/index.css` 中添加自定义 CSS 变量：

```css
@layer base {
  :root {
    --custom-color: #your-color;
  }
}
```

## 📖 相关文档

- [Tailwind CSS 官方文档](https://tailwindcss.com/docs)
- [shadcn/ui 官方文档](https://ui.shadcn.com/)
- [页面开发规范](mdc:.cursor/rules/page-development.mdc)
- [项目概览](mdc:.cursor/rules/project-overview.mdc)
