---
description: AI 协作总纲（规则分层、执行流程、质量保障与沟通约定）
alwaysApply: true
---

# AI 协作执行规则

## 规则分类

| 目录 / 文件 | 调用方式 | 说明 |
| ---- | ---- | ---- |
| [AGENTS.md](mdc:AGENTS.md) | 可选首读 | 仓库级技术栈摘要、文档与规则索引（与下表互补） |
| [basic/project-overview.mdc](mdc:.cursor/rules/basic/project-overview.mdc) | 建议首读 | 项目技术栈、目录分层、最佳实践与分层约束 |
| `basic/` | 必须调用 | 通用基础规范（类型、质量、样式、注释、命名、lint 等） |
| `modules/` | 按需调用 | 架构与分层（组件、页面、hooks、service、常量、工具、路由） |

## 执行流程

1. 识别场景并选择对应规则
2. 先读 `alwaysApply` 与场景规则，再开始改动
3. 对齐仓库现有实现风格，优先最小改动
4. 交付前完成受影响范围自检（类型、lint、构建按需）

## 强制行为

- 默认使用简体中文沟通
- 仅修改与任务直接相关的文件与行
- 具体技术约束以 `basic/*`、`modules/*` 为准

## 禁止行为

- 禁止无关重构、无关格式化与超范围改动
- 禁止在未阅读对应场景规则前直接编码
