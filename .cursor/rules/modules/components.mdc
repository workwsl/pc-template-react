---
description: 组件分层与复用边界
alwaysApply: false
---

# 组件规范

## 强制行为

- 跨页面复用组件放 `src/components`
- 页面私有组件放 `src/pages/<page>/components`
- 单个组件文件超过 300 行时，必须拆分子组件或 Hook
- 组件注释遵循 `basic/comment.mdc`；仅在组件职责、交互约束、副作用或拆分意图不直观时补充说明

## 禁止行为

- 禁止将页面私有组件过早上提为全局组件
- 禁止组件与页面编排逻辑混杂
- 禁止组件文件超过 300 行且无拆分
- 禁止为显而易见的 JSX 结构、className 或 props 传递逐行添加注释
