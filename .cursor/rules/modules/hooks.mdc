---
description: Hooks 放置与副作用边界
alwaysApply: false
---

# Hooks 规范

## 强制行为

- 自定义 Hook 以 `use` 开头
- 多页面复用 Hook 放 `src/hooks`
- 页面私有 Hook 放页面目录内，优先与页面私有组件同级或放在页面目录下的私有模块中
- Hook 只负责状态、数据编排与副作用管理；涉及 JSX 结构时应回到组件层
- 副作用需要清理，且依赖保持完整

## 禁止行为

- 禁止在 Hook 中混入页面渲染细节
- 禁止仅被单页面使用的 Hook 放入 `src/hooks`
